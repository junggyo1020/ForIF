<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Post</title>
    <link rel="stylesheet" href="css/css.css">
</head>
<body>
    <div class="board_wrap">
        <div class="board_title">
            <strong>My post</strong>
            <p>내 게시물을 수정합니다.</p>
        </div>
        <div class="board_write_wrap">
            <div class="board_write">
                <form action="/update/<%= post.id %>" method="POST">
                    <div class="title">
                        <dl>
                            <dt>제목</dt>
                            <dd><input type="text" placeholder="제목 입력" value="<%= post.title %>" name="title"></dd>
                        </dl>
                    </div>
                    <div class="subtitle">
                        <dl>
                            <dt>부제목</dt>
                            <dd><input type="text" placeholder="부제목 입력" value="<%= post.subTitle %>" name="subtitle"></dd>
                        </dl>
                    </div>
                    <!-- <div class="info">
                        <dl>
                            <dt>글쓴이</dt>
                            <dd><input type="text" placeholder="글쓴이 입력" value="<%= post.user_name %>" name="username"></dd>
                        </dl>
                    </div> -->
                    <div class="cont">
                        <textarea placeholder="내용 입력" name="body"><%= post.body %></textarea>
                    </div>
                    <div>
                        <select name="access" id="dropbox">
                            <option value="1" <%= post.access ? 'selected' : '' %>>공개</option>
                            <option value="0" <%= !post.access ? 'selected' : '' %>>비공개</option>
                        </select>
                    </div>
                    
                    </div>
                    <div class="bt_wrap">
                        <button class="btn">수정</button>
                    </div>
                </form>
            <a href="/main">취소</a>
        </div>
    </div>
</body>
</html>